import { ActionDeskClient } from '@/app/components/actionDesk/ActionDeskClient'

export const metadata = { title: 'AI Action Desk - Invozen GST' }

export default function ActionDeskPage() {
  return <ActionDeskClient />
}
