import { Suspense } from 'react'
import { VendorFormClient } from '@/app/components/vendors/VendorFormClient'

export default function NewVendorPage() {
  return <Suspense><VendorFormClient /></Suspense>
}
