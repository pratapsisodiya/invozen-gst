import { AICopilotClient } from '@/app/components/ai/AICopilotClient'

export const metadata = { title: 'AI Copilot — Invozen GST' }

export default function AICopilotPage() {
  return <AICopilotClient />
}
